/* is_buoyant : determines whether an object will float or sink in water */
int is_buoyant(double weight, double radius);

/* pods_to_order : determines the number of pods to order */
unsigned int pods_to_order(unsigned int people, unsigned int cups_per_day,
                           unsigned int days);

/* gcd : determines the greatest common denominator of two numbers */
unsigned int gcd(unsigned int n, unsigned int m);
